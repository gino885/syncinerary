"""Feature 2: the eval harness (CLAUDE.md section 12.3).

    python -m syncinerary.eval.runner

Fixtures in `fixtures/`, injectors in `disruption.py`, three scorer families
in `scorers.py`, and a runner that stores results by commit SHA so a run can
be diffed against the previous one.
"""
